<li>
  <%= icon_for(user) %>
  <%= link_to user.name, user %>
  <% if current_user.admin? %>
    | <%= link_to "eliminar", user, method: :delete,
                                  data: { confirm: "¿Seguro?" } %>
  <% end %>
</li>