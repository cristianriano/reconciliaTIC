<li>
  <% if user.picture? %>
    <%=link_to(image_tag(user.picture.url, width: 100, height: 100, alt: user.name), user) %>
  <% else %>
    <%= link_to image_tag("no_profile.jpg", alt: user.name, width: 100, height: 100), user %>
  <% end %>
  <%= link_to user.name, user %>
  <% if current_user.admin? %>
    | <%= link_to "eliminar", user, method: :delete,
                                  data: { confirm: "¿Seguro?" } %>
  <% end %>
</li>