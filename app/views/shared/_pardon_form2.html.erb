<%= form_for(@pardon) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Tambien perdono..." %>
    <%= f.hidden_field :kind, :value => "2" %>
  </div>
  <%= f.submit "Publicar", class: "btn btn-primary" %>
<% end %>