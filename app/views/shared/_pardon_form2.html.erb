<%= form_for(@pardon) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Perdono por..." %>
    <%= f.hidden_field :kind, :value => "2" %>
  </div>
  <%= f.submit "Perdonar", class: "btn btn-primary" %>
<% end %>